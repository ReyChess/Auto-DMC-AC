# Main benchmark outputs

Each dataset directory contains ten outer-fold rows in `dataset_summary.csv`, the complete ranked inner-selection log, and the CNS/PN feasibility logs. LetRecog, Mushroom, and PenDigits are archived reference-hardware outputs because their lowest-support candidates are sensitive to the documented runtime and memory guards. The remaining twelve directories were independently regenerated with source version 5.4.2.
